---
title: 'Using MDX and LaTeX'
description: 'A sample post demonstrating MDX and LaTeX capabilities.'
pubDate: '2025-12-07'
---

Testing MDX and Latex.

## Code Snippets

```python
import numpy as np

def gaussian(x, mu, sigma):
    return (1.0 / (np.sqrt(2 * np.pi) * sigma)) * \
           np.exp(-((x - mu)**2) / (2 * sigma**2))
```

```cpp
#include <iostream>

int main() {
    std::cout << "Hello, World!" << std::endl;
    return 0;
}
```

## Mathematical Equations

We can render beautiful equations using $\LaTeX$. For example, the Gaussian integral:

$$
\int_{-\infty}^{\infty} e^{-x^2} \, dx = \sqrt{\pi}
$$

Or the Schrödinger equation:

$$
i\hbar\frac{\partial}{\partial t}\Psi(\mathbf{r},t) = \hat H \Psi(\mathbf{r},t)
$$
